# This workflow will build and test a WPF desktop application
# built on .NET Framework 4.7.2

name: .NET Framework Desktop

on:
  push:
    branches: [ "TestsFinal", "master" ]
  pull_request:
    branches: [ "TestsFinal", "master" ]

jobs:

  build:

    strategy:
      matrix:
        configuration: [Debug, Release]

    runs-on: windows-latest

    env:
      Solution_Name: TumblThree.sln
      Test_Project_Path: TumblThree.Applications.Tests\TumblThree.Applications.Tests.csproj

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        submodules: recursive

    # Install .NET SDK (required for SDK-style projects like your test project)
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '6.0.x'

    # Add MSBuild to the PATH
    - name: Setup MSBuild.exe
      uses: microsoft/setup-msbuild@v2

    # Setup NuGet
    - name: Setup NuGet
      uses: NuGet/setup-nuget@v2

    # Restore NuGet packages
    - name: Restore NuGet packages
      run: nuget restore $env:Solution_Name

    # Build the solution
    - name: Build the solution
      run: msbuild $env:Solution_Name /p:Configuration=$env:Configuration /p:Platform="Any CPU" /maxcpucount
      env:
        Configuration: ${{ matrix.configuration }}

    # Run tests using dotnet test (SDK-style project)
    - name: Run tests
      run: dotnet test $env:Test_Project_Path --configuration $env:Configuration --no-build --logger "trx;LogFileName=test-results.trx" --results-directory TestResults
      env:
        Configuration: ${{ matrix.configuration }}

    # Upload test results
    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results-${{ matrix.configuration }}
        path: TestResults/*.trx
        if-no-files-found: warn

    # Upload build artifacts
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: TumblThree-${{ matrix.configuration }}
        path: |
          TumblThree.Presentation\bin\${{ matrix.configuration }}
        if-no-files-found: warn
